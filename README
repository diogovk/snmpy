This application aims at the development of Nagios plugins using the python language
and communication protocol SMNP (www.net-snmp.org) as transport.

1. Install dependencies (debian)

	1.1 apt-get install build-essential python-setuptools python-distutils-extra 
	    python-dev
	1.2 ldconfig
	1.3 Download the latest version of the sources of SNMP in 
	    http://sourceforge.net/projects/net-snmp/files/net-snmp/
	1.4 Recommended: Unzip and then compile with option "--with-python-modules"
	    OR: go to the python directory contained in the root directory and then 
	        follow the instructions in the README file 
	1.5 have a fun.
